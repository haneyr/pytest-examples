"""Add tests for adders here """
import pytest
from .. import module

@pytest.fixture(scope="session")

#Hint: Start by creating a fixture

#Create a test that adds 4 and asserts the product

#Create a test that adds 10 and asserts the product
